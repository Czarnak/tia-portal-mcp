# Acceptance Test Report — Phase 5 Reliability and Lifecycle Integrity

**Spec/Criteria:** `docs/superpowers/acceptance/2026-07-23-phase5-reliability-lifecycle-integrity.md` (Status: Approved)
**Plan under certification:** `docs/superpowers/plans/2026-07-23-phase5-04-certification-documentation.md`
**Report status:** Plan 4 Tasks 1–6 have recorded evidence: automated/documentation contracts, recorded user-accepted live TIA Portal V21 acceptance, the authorized source-skill update, documentation alignment, refreshed architecture graph, consolidated review, and final compiled-assembly test/coverage gates. The final serial solution build remains externally blocked by NuGet repository-signature TLS/authentication. More importantly, the consolidated security scan found one unresolved P2 item, so AC-041 is FAIL and the Phase 5 exit gate cannot certify the branch until it is remediated under its owning work.

---

## Commit / dependency / runtime identity

| Item | Value |
|---|---|
| Repo branch | `docs/phase5-04-certification-documentation` (based on `phase5`) |
| Host/worker source commit at time of this report | `a1624e8` ("docs: align phase 5 automated behavior") |
| Authorized source-skill update | `totally-integrated-claude` branch `codex/phase5-tia-mcp-skill`, commit `ed6ebce` ("docs: align TIA MCP skill with batch surface"); `scripts/validate-repo.ps1` passed. Installed plugin-cache files were not modified. |
| Installed/connected `tia-mcp` build actually exercised live | `2.3.2-local.45.gd73e95e` (commit `d73e95e`) |
| Production-code equivalence check | `git diff --stat d73e95e..a1624e8` touches only `README.md`, `docs/IMPROVEMENT_PLAN.md`, and `TiaMcpServer.Tests/*` — **zero changes** to `TiaMcpServer/`, `TiaMcpServer.Contracts/`, or `TiaMcpServer.OpennessWorker/`. The connected binary is therefore production-code-identical to repo HEAD for the purposes of this live run. |
| Windows version | Windows 11 Pro 10.0.26200.0 X64 (via `tia-mcp doctor --json`) |
| .NET runtime | .NET 8.0.29 (X64) |
| .NET Framework | 4.8.1 (release 533509) |
| TIA Portal product | "Totally Integrated Automation Portal" V21, `Siemens.Automation.Portal.exe` FileVersion/ProductVersion `2100.0.121.1`, installed at `C:\Program Files\Siemens\Automation\Portal V21` |
| `Siemens.Engineering.Base.dll` | FileVersion/ProductVersion `2100.0.121.1` |
| `Siemens.Engineering.Step7.dll` | FileVersion/ProductVersion `2100.0.121.1` |
| Worker executable resolved by doctor | `%USERPROFILE%\.dotnet\tools\.store\tiamcpserver\2.3.2-local.45.gd73e95e\...\openness-worker\TiaMcpServer.OpennessWorker.exe`, version `2.3.2-local.45.gd73e95e`, host/worker compatible |
| `tia-mcp doctor` result | 10/10 checks passed, 0 warnings, 0 failed |

## Disposable project provenance

| Item | Value |
|---|---|
| Disposable copy A (user-authorized) | `[local disposable-fixture root]\SimpleProject\SimpleProject.ap21` — reused from the same-day informal round-3 verification, explicitly re-authorized by the user for this formal run |
| SaveAs target B (user-authorized, verified empty before use) | `[local disposable-fixture root]\SimpleProject_SaveAsCopy\` — `Test-Path` confirmed absent before the SaveAs apply |
| Existing block used for update/edit round trip (user-authorized) | `PLC_1/Blocks/999_MISC/DamperDigital` (SCL FB) |
| New block path used for create-block test (user-authorized) | `PLC_1/Blocks/999_MISC/MCP_Cert_SCL_Test` (created as FC/SCL, verified, then deleted) |
| Audit directory | `%LOCALAPPDATA%\TiaMcpServer\audit` |
| Audit count before this test run | 7 files, 89 total JSONL lines |
| Audit count after this test run | 7 files, 102 total JSONL lines (+13: 4 lifecycle applies [close, open, save, save_as] + archive_project + 8 block-write batch applies, of which 3 were audited *failures* — see reconciliation below. Preview-only and pre-batch validation rejections outside a guarded apply added nothing, e.g. the `rebind:false` SaveAs rejection) |
| Audit reconciliation (post-review correction) | The pre-archive delta (+12) breaks down as 4 lifecycle applies (`close`✓ `open`✓ `save`✓ `save_as`✓) + 8 block-write batch applies, of which 3 failed (`roundtrip1`✗ `rt4`✗ `rt6`✓ `edit2`✓ `restore2`✓ `malformed1`✗ `create1`✓ `delete1`✓ — 5 successes + 3 failures = 8), i.e. 9 successes + 3 audited failures = 12 overall, plus the later `archive_project`✓ = 13 total. Verified against the raw `%LOCALAPPDATA%\TiaMcpServer\audit\2026-07-25.jsonl` entries, not the subagent's summary — see `.superpowers/sdd/task-2-steps1-4-evidence.md` |

Machine-specific canonical roots were recorded in the local acceptance evidence and are redacted from
this committed report to avoid leaking user-profile and project-location data. Stable fixture labels,
project names, versions, commands, and audit counts above remain sufficient to correlate the evidence.

---

## Criteria Results

Result legend: **PASS (live)** = directly exercised against real TIA Portal V21 in this session; "partial" or "precondition variant" suffixes flag where the live scenario actually run was narrower than, or different from, the AC's literal precondition — read the row's Evidence column for the exact gap. **PASS (automated)** = proven by the existing xUnit/FakeWorker suite (617/617 passing at `a1624e8` in both Debug and Release configuration — see Task 1 report and the AC-036 row); not re-derived live because the property is internal (e.g. worker-side timeout/crash injection, or a host-internal null-binding check) and not reproducible against a healthy live worker. **PENDING (Task N)** = requires a not-yet-executed task of this plan. **BLOCKED** = requires an external authorization not yet obtained.

| ID | Description | Test Type | Result | Evidence |
|----|-------------|-----------|--------|----------|
| AC-001 | Serialized MSBuild execution for CI/publish builds | Logic | PASS (automated) | Pinned by `CiWorkflowTests` (Task 1); `dotnet build TiaMcpServer.sln -m:1` used throughout |
| AC-002 | Coverage config scopes only approved assemblies | Logic | PASS (automated) | `TiaMcpServer.Tests/coverage.runsettings`, unchanged since Phase 5 Plan 1 |
| AC-003 | 80% scoped coverage threshold enforced | Logic | PASS (automated) | Task 6 scoped current-assembly run: line-rate **0.8578** ≥ 0.80, `verify-coverage-threshold.ps1` over the sole Cobertura result |
| AC-004 | Persistent two-process, single-request worker architecture preserved | Logic | PASS (automated + live) | `tia-mcp doctor`: host/worker version match, one persistent worker; live session used one continuous worker throughout Steps 3–6 |
| AC-005 | Exactly 10 MCP tools / 7 lifecycle tools, no internal leakage | Logic | PASS (automated) | `McpToolSchemaTests`/`WriteToolSafetyTokenTests` (Task 1); doctor/tool listing consistent |
| AC-006 | Direct status for B cannot open/switch away from open A | API | **PASS (live, precondition variant)** | AC's stated precondition is "MCP session is unbound." What was actually run: `get_project_status(projectPath=<B path>)` **while the session was already bound to A** → `failureCategory:"binding_conflict"`, message names `open_project`; follow-up `get_project_status()` confirmed A still open, unchanged. This proves the bound-session guard correctly refuses to switch — a real safety proof — but is not the AC's literal unbound-session scenario; that exact precondition was not separately reproduced live (would require a project opened outside this MCP session's own lifecycle calls) and remains covered by the existing automated `ProjectOpenPolicy` tests |
| AC-007 | Direct status does not open a supplied project when none is open | API | **PASS (live)** | Corrected during re-review: the original evidence only covered the no-path trivial case. Re-tested the AC's actual scenario — closed the open project, then `get_project_status(projectPath=<a different, valid, real project path>)` while nothing was open → `{"isOpen":false}`, the supplied project was **not** opened; a follow-up no-path call confirmed no side effect. Session left unbound, nothing open, at the end of Task 2 |
| AC-008 | Direct status inspects only the already-open project | API | **PASS (live, precondition variant)** | Same caveat as AC-006: tested while the session was bound to A (`get_project_status()` with no `projectPath` → reports A, no switch command issued), not in the AC's literal unbound state. The precise unbound scenario is covered by existing automated coverage |
| AC-009 | Guarded lifecycle writes use internal probe, not public status | API | PASS (automated) | Proven by FakeWorker operation-name assertions (Plan 2 Task 2, pre-existing); live `save_project`/`close_project`/`open_project` applies in this session behaved consistently (embedded `verification` object, no observable public-status side effect) — the exact RPC-name separation is not independently observable from outside the process |
| AC-010 | Guarded lifecycle preview/apply flows remain functional post-split | API | **PASS (live)** | All four named flows exercised: `close_project`, `open_project`, `save_project`, `save_project_as` (each producing a preview token and succeeding on matching apply), plus `archive_project` (added after review flagged its absence) — preview token `MoLJ...`, apply succeeded, archive file confirmed on disk (2,179,120 bytes) |
| AC-011 | Failed lifecycle call does not change session binding | Logic | PASS (automated) | Pre-existing Plan 2 coverage, unchanged |
| AC-012 | Successful read does not bind an unbound session | Logic | **PASS (live, indirect) + automated** | The very first `get_project_status()` call of this test run (before any `open_project`/`create_project`/`save_project_as` call in this session) returned a **successful response with a resolved path** (`isOpen:true`, path = A, left open from prior same-day testing) while the session was necessarily still unbound. This is consistent with the AC but not conclusive proof on its own — the internal `BoundProjectPath` staying null is not independently observable from the MCP client, and no live signal in this session discriminates "stayed unbound" from "silently bound." Pre-existing Plan 2 automated coverage remains the authoritative proof |
| AC-013 | Successful lifecycle transitions bind only to worker-reported ground truth | Logic | PASS (automated) | Pre-existing Plan 2 coverage; live `open_project`/`save_project_as` bound exactly to the worker-reported resolved path (A, then B) |
| AC-014 | Rebind-required transition fails without resolved path | Logic | PASS (automated) | Pre-existing Plan 2 coverage, unchanged |
| AC-015 | `save_project_as(rebind:false)` rejected before preview/execution | API | **PASS (live)** | Live call → `failureCategory:"validation_error"`, no `safetyToken` field, audit count unchanged (92→92), A remained open |
| AC-016 | Supported SaveAs binds host+worker to copied project | API | **PASS (live)** | `save_project_as(rebind:true)` preview→apply → success; copied file confirmed on disk (`Test-Path` = True); `get_project_status()` post-apply reports B; audit +1 |
| AC-017 | Failed SaveAs preserves original binding | API | PASS (automated) | Pre-existing Plan 2 coverage (FakeWorker), unchanged |
| AC-018 | SaveAs copied-path discovery failure reported as unverifiable | Logic | PASS (automated) | Pre-existing Plan 2 coverage, unchanged |
| AC-019 | Successful direct lifecycle responses preserve warnings | API | PASS (automated) | Pre-existing Plan 2 coverage, unchanged |
| AC-020 | Warnings do not mask lifecycle failures | Logic | PASS (automated) | Pre-existing Plan 2 coverage, unchanged |
| AC-021 | Warning propagation obeys response budget | Logic | PASS (automated) | Pre-existing Plan 2 coverage, unchanged |
| AC-022 | Safety-token protections unchanged by lifecycle routing | API | **PASS (live + automated)** | Live: reused SaveAs token rejected (`"Safety token expired, consumed, or unknown."`); missing/invalid/confirm-false/reorder cases remain covered by pre-existing automated suite |
| AC-023 | Bundle parsing rejects missing/duplicate documents pre-import | Logic | **PASS (live, partial) + automated** | Live: duplicate-document-name bundle against `DamperDigital` → immediate structural rejection, no Siemens import, block unchanged (see `.superpowers/sdd/task-2-blocks-report.md`). Missing-document case covered by existing automated `BlockImportBundleParser` tests |
| AC-024 | Staging rejects unsafe filenames/paths | Logic | PASS (automated) | Live run exercised the duplicate-name case only — Plan 4 Task 2 Step 5.4 permits submitting "one malformed/unsafe bundle"; unsafe-filename/path-traversal cases remain covered by existing automated `BlockImportStager` tests |
| AC-025 | Deterministic exact staging filenames | Logic | PASS (automated) | Worker-internal property, not observable via the MCP client surface; existing automated coverage is authoritative |
| AC-026 | Byte-identical `update_block_logic` round trip succeeds | API | **PASS (live, partial)** | Two attempts to resubmit the full exported 3-document bundle (`.xml`+`.s7dcl`+`.s7res`) unmodified were **rejected** by the non-authoritative-document guard (`'DamperDigital.s7dcl' was modified, but only 'DamperDigital.xml' is applied...`), most likely because the manually-transcribed `.s7dcl` did not byte-match live state (a documented trailing-whitespace transcription issue, not a reproduced product defect). The eventual success submitted **only the `.xml` document**, which the guard accepts (nothing to compare for omitted documents) — `Import succeeded`, `compile_check` 0 errors, re-export SHA/diff-confirmed the `.xml` byte-identical. So: the authoritative document's byte-identical round trip is proven; the AC's literal "submit the unmodified bundle" (all three documents at once) was not achieved live. **Open question for Task 4:** a *programmatic* byte-exact resubmission of the exported `.s7dcl` (not manually re-typed) was never attempted, so an export/import asymmetry in `get_block_content`/`update_block_logic` for non-authoritative documents is not actually ruled out — Task 4 should not treat this as fully closed without either that programmatic test or an explicit decision to accept the authoritative-document-only proof as sufficient. See `.superpowers/sdd/task-2-steps1-4-evidence.md` for the exact failing error text and timestamps |
| AC-027 | Valid edited round trip succeeds and compiles | API | **PASS (live)** | Single-line `<Text />`→`<Text>MCP_CERT_ROUNDTRIP_MARKER</Text>` edit applied once, `Import succeeded`, `compile_check` 0 errors, re-export confirmed the marker present and surrounding logic (`DoubleSignalTON`, 6 occurrences) intact |
| AC-028 | Malformed input never succeeds and never alters the original block | API | **PASS (live)** | Duplicate-document-name submission failed deterministically; follow-up export confirmed the block byte-identical to its pre-attempt state |
| AC-029 | Failed postcondition verification is failure, not warning-only success | Logic | PASS (automated) | Live reproduction judged too risky to attempt without a reliable restore path (documented rationale in `.superpowers/sdd/task-2-blocks-report.md`); existing FakeWorker-based coordinator tests (Plan 3) are authoritative |
| AC-030 | SCL generation always yields a valid non-empty compile unit | Logic | **PASS (live, one type + automated)** | Live `create_block(FC, SCL)` produced a schema-valid, self-closing `<StructuredText .../>` compile unit, matching the Task 6 design fix from `priv/MCP_TOOL_TEST_REPORT_ROUND3.md`. AC requires "each Phase 5-supported block type"; only FC was exercised live this session (round 3 additionally covered GlobalDB the same day). FB/OB remain covered by existing automated `BlockSourceGenerator`/`BlockSourceValidator` tests |
| AC-031 | SCL `create_block` succeeds and compiles against V21 | API | **PASS (live)** | `create_block(PLC_1/Blocks/999_MISC/MCP_Cert_SCL_Test, FC, SCL)` → success; resolved via `browse_project_tree`; `compile_check` 0 errors/0 warnings; cleaned up via `delete_block`, final whole-project `compile_check` 0/0 |
| AC-032 | Failure categories serialize/render deterministically | Logic | PASS (automated) | Pre-existing Plan 2 coverage, unchanged; live session observed `validation_error`, `binding_conflict` renderings consistent with spec |
| AC-033 | Write never auto-retried after worker timeout | API | PASS (automated) | Not producible against a healthy live worker; existing FakeWorker-based test is authoritative |
| AC-034 | Write never auto-retried after crash/protocol loss | API | PASS (automated) | Not producible against a healthy live worker; existing FakeWorker-based test is authoritative |
| AC-035 | Enumerated recoverable failures need no manual TIA recovery | API | **PASS (live, partial — 3 of 5)** | Covered live: status binding conflict ✓, rejected `rebind:false` ✓, malformed bundle rejected before import ✓ — after each, the very next same-session call succeeded with no manual TIA Portal intervention. Not covered live: "invalid SaveAs input" beyond `rebind:false` itself, and "Siemens import failure with worker connected" (the two block-write host-side guard rejections were pre-Siemens validation, not a Siemens-layer import failure). Remaining scenarios covered by existing automated coverage |
| AC-036 | Full automated + Round 4 regression suites remain green | API | **PASS (Task 6 compiled assembly)** | Current `TiaMcpServer.Tests.dll` executed directly through VSTest: **619/619 passed**, 0 failed/skipped. The earlier focused TDD compilation included the review fix (`3813784`); later commits before this run were graph/report only. `dotnet restore` succeeded, but the required serialized solution build was blocked externally by NuGet repository-signature `NU1301` TLS/authentication even with `--no-restore`; this is recorded as a Task 6 infrastructure blocker rather than a test failure. |
| AC-037 | Live acceptance evidence identifies actual Siemens runtime | API | **PASS** | This report's "Commit / dependency / runtime identity" and "Disposable project provenance" sections |
| AC-038 | Repository documentation matches delivered behavior | Logic | **PASS (Task 4)** | README now records verified multi-document `update_block_logic`, SCL `create_block`, SaveAs rebind, and recovery behavior without the obsolete pending-live wording. `IMPROVEMENT_PLAN.md` closes the documentation work while preserving its Phase 6+ exclusions. The focused documentation/schema filter passed 41/41. |
| AC-039 | Source skill documentation updated without touching installed cache | Logic | **PASS (Task 3)** | User supplied and authorized the owning `totally-integrated-claude` checkout. Commit `ed6ebce` documents the ten-tool batch/lifecycle surface, self-previewing lifecycle writes, non-binding status, `rebind:true`, failure categories/warnings, and block-write behavior; its repository validator passed. The installed cache was not modified. |
| AC-040 | Graphify artifacts reflect final code state | Logic | **PASS (Task 5)** | `graphify update .` after the reviewed lifecycle-preview envelope fix rebuilt 1,767 nodes, 2,544 edges, and 202 communities. Generated `graphify-out/` artifacts committed at `ff938ee`. Graph-freshness proof will be rerun after the Task 6 report-only commit. |
| AC-041 | No hardcoded secrets/protected-data leakage introduced | Logic | **FAIL (Task 5 security scan)** | Sealed Codex Security diff scan of `f0b81c8..1a27765` closed all 60 worklist rows and found no secret, cache-boundary, retry, or false-success issue, but reported **P2 / medium** `SEC-P5-W1B-001`: raw postcondition `Exception.Message` is concatenated into a caller-visible MCP error (`BlockExporter.cs:43`, `BlockExporterVerification.cs:43`, `BlockPostconditionVerifier.cs:26–29`). The local-stdio boundary limits exposure, but the disclosure remains unresolved. Per Task 5, do not patch it inside certification; route it to its owning block-write work before certification can pass. |
| AC-042 | Changed external inputs validated at host/worker boundaries | Logic | **PASS (live + automated)** | Live: `rebind:false` and the duplicate-document bundle both rejected before any Siemens call; existing automated negative-path tests cover the rest |
| AC-043 | Audit semantics preserved; no uncertain-outcome false success | Logic | **PASS (live, partial — successful + validation-failed only)** | Every failed call this session returned an explicit failure category (`validation_error`/`binding_conflict`), never partial/ambiguous success — confirmed both in tool responses and in the raw audit JSONL, which records failed guarded-write attempts as `success:false` (not omitted, not misreported). AC's full precondition also lists worker-failed, timeout/crash, and postcondition-failed guarded writes, none of which occurred against a healthy live worker in this session; those remain covered by existing FakeWorker-based automated tests |
| AC-044 | Phase 6 capabilities stay outside Phase 5 scope | Logic | PASS (automated) | Task 1's `IMPROVEMENT_PLAN.md` additions explicitly keep Transactions/ExclusiveAccess/Openness events/auth expansion/doctor enhancements as Phase 6+; no such capability appears in this branch's diff |
| AC-045 | Bound status read surfaces worker-path divergence without adopting it | Logic | PASS (automated) | Not exercised live (requires deliberately desynchronizing host/worker binding); pre-existing Plan 2 `WarnOnBindingDivergence` coverage is authoritative |

## Summary counts

Recounted directly from the table above after the post-review corrections (no double-counting of ranges, AC-009/AC-036/AC-037 no longer folded into the "live" bucket):

- **Total criteria:** 45
- **PASS, with a live TIA Portal V21 component this session (full or partial — see each row for exact scope):** 16 — AC-006, AC-007, AC-008, AC-010, AC-015, AC-016, AC-022, AC-023, AC-026, AC-027, AC-028, AC-030, AC-031, AC-035, AC-042, AC-043. Several are explicitly marked "partial" or "precondition variant" in their row — count as PASS-with-caveat, not unconditional PASS.
- **PASS, via this session's own automated command execution (not live TIA, but freshly re-run — the Release-config gap closed during review):** 1 — AC-036
- **PASS, satisfied by this report's own recorded runtime/provenance data:** 1 — AC-037
- **PASS, automated-primary (pre-existing suite or Task 1; a few of these rows — AC-004, AC-012, AC-013 — also cite supporting live observations, but the automated suite is the authoritative proof):** 23 — AC-001–005, AC-009, AC-011–014, AC-017–021, AC-024, AC-025, AC-029, AC-032–034, AC-044, AC-045
- **PASS, documentation/source-skill (Tasks 3–4):** 2 — AC-038, AC-039
- **PASS, graph/review infrastructure (Task 5):** 1 — AC-040
- **PENDING:** 0
- **FAIL:** 1 — AC-041 (unresolved P2 diagnostic disclosure)
- Total: 16 + 1 + 1 + 23 + 2 + 1 + 1 = 45 ✓

## Task 5–6 finalization evidence

- **Consolidated review:** code review identified an uncategorized lifecycle preview-read failure. Regression test first failed while parsing the returned bare text; `3813784` routes that path through `BuildApplyResult`, and its focused xUnit test then passed. No second whole-branch code review was run, honoring the requested single-review cadence.
- **Security review:** completed and contract-validated Codex Security branch-diff scan of `f0b81c8..1a27765`. It has 60/60 closed worklist rows and one reportable medium-confidence local diagnostic-disclosure finding (`SEC-P5-W1B-001`); all other candidates were suppressed or explicitly deferred with receipts. No TIA Portal process or live test was used.
- **Graph freshness:** `graphify update .` rebuilt 1,767 nodes, 2,544 edges, and 202 communities, committed as `ff938ee`. `git diff --quiet ff938ee HEAD -- '*.cs'` exited 0 before the final report-only commit; no C# file changed after the graph refresh.
- **Fresh automated gates:** elevated `dotnet restore TiaMcpServer.sln` succeeded. Direct VSTest of the current `TiaMcpServer.Tests.dll` passed 619/619, and the coverage collector produced one Cobertura report with line-rate 0.8578. The solution-level `dotnet build ... -m:1 /p:UseTiaPortalReferenceStubs=true` could not complete because NuGet repository-signature lookup failed with `NU1301` TLS/authentication even after the successful restore; no code compilation error was reported.
- **Live/API exception:** no live TIA Portal or Openness API scenario was rerun in Task 6, per the user's explicit instruction. Task 2's recorded V21 evidence remains historical/accepted evidence, not a fresh live retest.

## Residual state (as of end of Task 2)

- **Project A** (`...\SimpleProject\SimpleProject.ap21`): closed, then reopened and saved during Step 3; not otherwise modified. Left closed-then-reopened is fine — it is the user-authorized disposable copy and remains available for reuse.
- **Project B** (`...\SimpleProject_SaveAsCopy\SimpleProject_SaveAsCopy.ap21`): created via SaveAs during Step 4, left open and bound at the end of this session. `PLC_1/Blocks/999_MISC/DamperDigital`'s `.xml` document is byte-identical to its state at export time (SHA/diff-verified); its `.s7dcl`/`.s7res` companion documents carry Siemens-regenerated `MLC_*` comment-resource IDs as a side effect of the reduced-bundle round-trip technique described under AC-026 — comment **text** is unchanged, `compile_check` is 0/0, but project B is not byte-identical to its state immediately after the SaveAs copy. `PLC_1/Blocks/999_MISC/MCP_Cert_SCL_Test` was created, exercised, and deleted — confirmed no residue via a final whole-project `compile_check` (0 errors/0 warnings, both PLCs). An archive (`SimpleProject_SaveAsCopy_Archive`, 2,179,120 bytes) was also created in the same parent directory during the AC-010 fix.
- Project B and its archive are test artifacts of this run, not needed for any later task in this plan. Neither was deleted — left in place in case Task 6's rerun wants to reuse or compare against them; the user may delete them at their discretion.
- No other project, block, table, or device was touched.

## Outstanding work (not claimed complete by this report)

- **Remediation prerequisite for certification** — `SEC-P5-W1B-001` must be fixed and verified in its owning block-write work before AC-041 and the Phase 5 exit gate can pass. The completed scan report is retained as a local Codex Security artifact; no TIA Portal interaction was used during the scan.
- **Build-environment prerequisite for a fully fresh clean build** — resolve NuGet repository-signature TLS/authentication (`NU1301`), then rerun the serial stub build. The direct current-assembly test and coverage gates passed, but this does not convert the failed build infrastructure step into a passing build.

**Evidence-file caveats (for whoever picks this up next):**
- The raw evidence files this report cites (`task-2-steps1-4-evidence.md`, `task-2-blocks-report.md`, and `priv/MCP_TOOL_TEST_REPORT_ROUND3.md`) live under gitignored paths (`.superpowers/sdd/*` is scratch by design; `priv/` is ignored separately) and will not exist in the repo once committed. The load-bearing specifics (exact error text, timestamps, byte counts, command output) have been quoted verbatim inline in this report's table wherever they support a PASS verdict, so this report should be self-sufficient even if those files are later cleaned up — but if anything here needs re-verification, regenerate it live rather than assuming the scratch files still exist.
- `.superpowers/sdd/` also contains artifacts from an *earlier, different* plan (e.g. `task-2-brief.md` belongs to the block-write-format-repair plan's own Task 2, and `task-3-report.md`/`task-6-report.md` are from the batch-operations feature work) — none of those are evidence for this plan's Task 2/3/6. This report's own raw-evidence files are named `task-2-steps1-4-evidence.md` and `task-2-blocks-report.md` specifically to reduce ambiguity, but a reader browsing that directory should not assume every `task-N-*.md` file belongs to this plan.

## Overall verdict

**Task 2 (live TIA Portal V21 acceptance) scope: PASS, with documented caveats.** Every criterion within Task 2's stated ownership (live/API portions of AC-006–AC-010, AC-015–AC-016, AC-026–AC-028, AC-031, AC-035, AC-037, plus live confirmation of AC-041–AC-043 to the extent observable) was exercised against real TIA Portal V21 on the user-authorized disposable project. None failed outright, but three rows carry a materially narrower scope than a first pass claimed and were corrected after task review: AC-026 (only the authoritative `.xml` document was proven byte-identical, not the full 3-document bundle — two full-bundle attempts failed first), AC-006/AC-008 (tested with the session already bound, not in the AC's literal unbound-session precondition), and AC-010/AC-035/AC-043/AC-030 (each narrower than originally stated — see table). AC-036 required an additional Release-configuration run (discovered missing during review) which now passes cleanly (617/617, coverage 0.8671).

**Phase 5 is not certified.** Task 5 completed one consolidated review. Its code-review finding — lifecycle preview read failures were returned as bare text rather than structured envelopes — was corrected test-first in `3813784` and the focused xUnit regression test passed. Task 6 then passed 619/619 current-assembly tests and 0.8578 scoped coverage without rerunning any live test. The review also produced unresolved P2 `SEC-P5-W1B-001`, so AC-041 is FAIL; additionally, the fresh serial solution build is blocked by external NuGet repository-signature TLS/authentication. The user-directed no-live-test exception is fully recorded above.
